# ---------------------------------------------------------------------------
# Ortam değişkenleri şablonu.
# Bu dosyayı `.env.local` olarak kopyala ve değerleri doldur.
# `.env.local` git tarafından YOK SAYILIR — gerçek anahtarları oraya yaz,
# asla bu repoya commit'leme.
# ---------------------------------------------------------------------------

# "Yapay Alper" sohbet asistanı için Anthropic API anahtarı.
# YALNIZCA sunucu tarafında kullanılır — NEXT_PUBLIC_ ÖN EKİ KULLANMA!
# Anahtar al: https://console.anthropic.com
ANTHROPIC_API_KEY=
